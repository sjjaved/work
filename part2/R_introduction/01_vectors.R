# Create a vector of numbers
numbers = c(1, 2, 3, 4, 5)
print(numbers)

# Create a vector of letters
ltrs = c('a', 'b', 'c', 'd', 'e')
ltrs

# Concatenate both 
mixed_vec = c(numbers, ltrs)
mixed_vec

